{
  "name": "@master-software/load-credentials-action",
  "description": "Loads credentials from a secure vault",
  "version": "1.0.0",
  "scripts": {
    "create:tag": "git tag -a v$(cat package.json | jq -r '.version') -m v$(cat package.json | jq -r '.version') && git push --follow-tags"
  }
}